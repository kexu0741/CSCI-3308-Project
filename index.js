module.exports = function(width, height) {
    return width * height;
  };

  //To test in terminal type: node
  //var area = require('./index.js');
  // result should be undefined.
  // now run area(2,4) -> should return 8